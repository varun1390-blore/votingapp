apiVersion: v1
kind: Pod
metadata:
  name: worker-app-pod
  labels:
    name: worker-app-pod
    app: demo-voting-app
Spec:
  containers:
  - name: worker-app
    image: docker pull dockersamples/examplevotingapp_worker
